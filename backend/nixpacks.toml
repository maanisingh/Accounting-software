[phases.setup]
nixPkgs = ['nodejs_22', 'openssl']

[phases.install]
cmds = ['npm install --legacy-peer-deps']

[phases.build]
cmds = [
  'npx prisma generate'
]

[start]
cmd = 'npx prisma db push --accept-data-loss && node prisma/seed.js && node src/server.js'
